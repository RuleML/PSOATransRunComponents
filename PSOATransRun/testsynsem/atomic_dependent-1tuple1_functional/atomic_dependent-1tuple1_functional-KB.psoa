RuleML(
  Assert(
    % atomic-formula expressivity
    % relationships
    % single tuple
    %   dependent tuple
    %     explicit tuple
    %   3 elements
    %     constant elements
    p(+[q(+[a])])
    % '_p'('_q'('_a')).
) )

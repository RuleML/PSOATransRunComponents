RuleML(
  Assert(
    % atomic-formula expressivity
    % 2 relationships
    % single tuple
    %   dependent tuple
    %     explicit tuple
    %   1 element
    %     constant elements
    p(a)
    p(b)
) )